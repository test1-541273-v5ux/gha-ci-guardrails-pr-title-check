name: CI Guardrails - PR Title Check
description: Validates pull request title format in CI.
author: YourCompany
branding:
  icon: play
  color: blue
inputs:
  message:
    description: Optional message to print
    required: false
    default: Hello from CI Guardrails - PR Title Check
runs:
  using: node20
  main: dist/index.js
